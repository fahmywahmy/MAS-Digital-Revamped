"""MAS Digital Revamped — Python toolchain (agency-tool rebuild).

Vertical slices live under here (research / creative / seo / paid). Everything
that talks to Anthropic does so through `tools.utils.claude_client` — the single
gateway door (CI-enforced). See docs/ARCHITECTURE.md.
"""
